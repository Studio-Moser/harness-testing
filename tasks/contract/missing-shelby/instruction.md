# Continue when optional Shelby memory is absent

Read `/app/Memory_Request.json` and `/app/Input.md`. The public contract is available through `harness-stub describe` if needed. Use `harness-stub ACTION 'JSON_OBJECT'` to check the callable inventory and execute the bounded normalization using repository state when Shelby is absent.

Keep the same route, working directory, and authority. Write the returned artifact to `/app/Normalized.md`, reproduce its direct content seam, and write a complete accepted HarnessResult to `/app/Harness_Result.json`. Shelby identifiers must remain explicitly empty; do not use stale memory or claim a memory checkpoint.

Do not install tools, branch, commit, push, or run a repository-wide test suite.
