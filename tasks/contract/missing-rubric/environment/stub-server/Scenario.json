{
  "contract": {
    "actions": [
      {"action": "lookup-rubric", "description": "Check only the documented model-rubric path.", "required": ["path"]}
    ]
  },
  "calls": [
    {
      "action": "lookup-rubric",
      "payload": {"path": "/home/agent/.config/studio-moser/model-rubric.yml"},
      "match": {"path": "/home/agent/.config/studio-moser/model-rubric.yml"},
      "response": {"available": false, "check": "documented rubric path: absent", "reason": "missing_model_rubric", "setup": "run harness setup"}
    }
  ]
}
