# Prove a UI state through computer use

Read `/app/Computer_Use_Request.json`. Use the configured `computer-use` MCP capability—not code inspection—to open the local test UI, select the Navy theme, capture a PNG, and inspect that capture.

This request is outside project-management lifecycle. Preserve the exact target state and use the available callable capability directly. Write the complete public HarnessResult to `/app/Harness_Result.json`, naming the protected sidecar screenshot and fresh visual checks.

If computer use is unavailable, return a typed blocked result. Do not substitute source inspection, install tools, edit code, branch, commit, push, or run tests.
