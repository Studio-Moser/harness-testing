Work in `/app` and complete these static-page updates:

- Set the hero heading to `Build calmer workflows`.
- Set `--section-gap` to `4rem`.
- Wrap the primary content in `<main id="main-content">`.

Preserve existing project tests, package metadata, build configuration, and check scripts. Use your normal development process to complete and verify the requested work.
