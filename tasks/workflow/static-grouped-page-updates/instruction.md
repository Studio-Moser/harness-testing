Complete all three static-page updates before the final gate:

- Set the hero heading to `Build calmer workflows`.
- Set `--section-gap` to `4rem`.
- Wrap the primary content in `<main id="main-content">`.

You may use `npm run check:hero`, `npm run check:spacing`, and `npm run check:main`
for focused feedback. Run `npm run gate` exactly once after all edits are complete. Do
not run the gate between items.
