Work in `/app` and complete saved dashboard views for the `all`, `active`, and `archived` states. Persist the current view under `dashboard.saved-view`, restore it on reload, and fall back to `all` when storage is absent or invalid. Keep storage access behind the existing saved-view functions.

Preserve existing project tests, package metadata, build configuration, and check scripts. Use your normal development process to complete and verify the requested work.
