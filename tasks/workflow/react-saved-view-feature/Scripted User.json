{
  "schema_version": "1",
  "interaction_limit": 12,
  "facts": {
    "workspace": "Work in /app.",
    "task": "The existing UI and domain model already define the `all`, `active`, and `archived` states. Automatically save each selection immediately, including `all`, as its exact lowercase string under `dashboard.saved-view`; restore it on reload. Accept only those three exact strings: no trimming, case conversion, or JSON encoding. For absent, empty, or invalid values, display `all` and leave storage untouched until the user selects a view. Keep storage access behind the existing saved-view functions. If getItem throws, silently return `all`; if setItem throws, silently keep the selected view in memory without throwing or showing a warning. Storage failures here mean exceptions from these existing methods; do not expand this into a new storage API or error UI. These choices are approved for the original local task.\n\nPreserve existing project tests, package metadata, build configuration, and check scripts. Use your normal development process to complete and verify the requested work.\n",
    "authority": "Proceed with the original local development task within its stated scope. No additional spending, external publication, messages, or authority is approved.",
    "handoff": "Keep the branch and workspace as-is. Do not merge, push, publish, discard changes, or remove the worktree. Report the local handoff and stop."
  },
  "rules": [
    {
      "id": "saved-view-product-choices",
      "kind": "clarification",
      "pattern": "(?is)(?=.*\\?)(?=.*\\b(?:view|persist|storage|reload|default|fallback|missing|absent|invalid|saved)\\b).*",
      "fact": "task"
    },
    {
      "id": "workspace",
      "kind": "clarification",
      "pattern": "(?:Where should I (?:work|make (?:the|these) changes)|What (?:is the )?working directory should I use)\\?",
      "fact": "workspace"
    },
    {
      "id": "task-scope",
      "kind": "clarification",
      "pattern": "(?:What (?:is|are) the (?:task|requirements)|What should I (?:build|change))\\?",
      "fact": "task"
    },
    {
      "id": "routine-plan",
      "kind": "approval",
      "matcher": "local-development-approval",
      "fact": "authority"
    },
    {
      "id": "local-handoff",
      "kind": "clarification",
      "matcher": "local-branch-handoff",
      "fact": "handoff"
    }
  ]
}
