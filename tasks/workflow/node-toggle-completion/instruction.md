Work in `/app`. Implement toggleDone(task) in src/Completion.js. Return a new object with done toggled and every other property preserved. Never mutate the input. The input is a task object whose done property is boolean. No persistence, UI, dependencies, or additional validation is needed.

Preserve existing tests, package metadata and build configuration. You may add a separate regression test. Complete the local task; do not publish or deploy.
