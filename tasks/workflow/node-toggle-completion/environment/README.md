# Immutable completion toggle

Frozen atomic task. Run `npm test` for the existing check. Only `src/Completion.js` is an existing mutable file; new regression tests may be added separately. Do not change existing tests or runner configuration.
