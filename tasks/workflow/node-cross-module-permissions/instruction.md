Work in `/app`. A user whose role changes can still modify invoices from background work, and visiting another workspace can change what the HTTP route allows. Diagnose the shared cause and fix both paths without removing the cache. Revocation and promotion must apply immediately; no role in one workspace grants access in another. Add relevant regression coverage.

Preserve existing tests, package metadata, and build configuration. You may add implementation and regression files. Complete and verify the local work; do not publish or deploy.
