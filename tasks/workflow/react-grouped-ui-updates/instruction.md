Work in `/app` and complete all three independent dashboard updates:

1. Change the `--accent` custom property from `#2563eb` to `#6d28d9`.
2. Change the empty-state heading to `No projects yet`.
3. Change the `--card-gap` custom property from `20px` to `12px`.

You may use `npm run check:accent`, `npm run check:copy`, and
`npm run check:spacing` while making the individual updates. After all three updates
are complete, run `npm run gate` exactly once. The full gate is forbidden between
updates because this is a grouped assignment.

Do not modify the project tests, package metadata, build configuration, or check
scripts. Keep the change limited to the requested source values.
