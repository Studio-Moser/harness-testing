Update the pricing card without changing behavior elsewhere:

- Change its sentence to `Everything your team needs to ship.`
- Set `--pricing-card-gap` to `1.5rem`.

Run `npm run check:pricing-card` to verify the finished edit. This is a polish task;
do not run the package test suite or `npm run gate`.
