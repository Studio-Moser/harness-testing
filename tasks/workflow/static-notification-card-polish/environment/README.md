# Notification card

Edit only Styles.css. Run `python Visual_Check.py`; screenshots go to /tmp/visual-proof. Preserve the supplied markup, copy and Save interaction. No dependencies or network are needed.
