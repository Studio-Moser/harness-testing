Work in `/app` and fix `selectActiveCount` so archived projects never contribute to the active badge, even when their `active` field is true. Add regression coverage for this behavior. Keep the production change limited to active counting.

Preserve existing project tests, package metadata, build configuration, and check scripts. Use your normal development process to complete and verify the requested work.
