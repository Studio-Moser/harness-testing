# Inclusive capacity boundary

Frozen atomic task. Run `npm test` for the existing check. Only `src/Capacity.js` is an existing mutable file; new regression tests may be added separately. Do not change existing tests or runner configuration.
