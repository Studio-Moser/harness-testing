Work in `/app`. Fix isAtCapacity(count, limit) in src/Capacity.js. A queue at exactly its capacity must count as full, as must an over-capacity queue. Below capacity must remain false. Inputs are nonnegative integers; no new validation or API is needed. Preserve the existing public export.

Preserve existing tests, package metadata and build configuration. You may add a separate regression test. Complete the local task; do not publish or deploy.
