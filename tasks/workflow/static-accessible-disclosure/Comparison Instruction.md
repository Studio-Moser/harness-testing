Work in `/app` and repair the FAQ disclosure so mouse and keyboard users get the same state. Click, Enter, and Space must toggle the answer while keeping the trigger’s `aria-expanded` and the panel’s `hidden` state synchronized.

Preserve existing project tests, package metadata, build configuration, and check scripts. Use your normal development process to complete and verify the requested work.
