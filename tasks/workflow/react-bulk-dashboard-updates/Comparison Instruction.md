Use the Harness `bulk` route for implementation. This task intentionally fixes the semantic route so the active model rubric, rather than the root agent's classification judgment, selects the child model.

Work in `/app` and complete these dashboard updates:

- Change `--accent` from `#2563eb` to `#6d28d9`.
- Set the empty-state heading to `No projects yet`.
- Change `--card-gap` from `20px` to `12px`.

Keep edits limited to the requested source values.

Preserve existing project tests, package metadata, build configuration, and check scripts. Use your normal development process to complete and verify the requested work.
